{
  "_note": "Cloud routine spec for the closed seam's keyless half (decision-log 2026-07-17). Create via the schedule skill / RemoteTrigger once GitHub is connected to claude.ai code. VPS half (cron-foundry.sh) is already live.",
  "name": "foundry-implementer",
  "cron_expression": "0 8,20 * * *",
  "enabled": true,
  "model": "claude-sonnet-5",
  "repo": "https://github.com/pwysocan-droid/algotrading-paper",
  "prompt": "You are the foundry implementation agent — the keyless cloud half of the closed seam. Read decision-log.md entry '2026-07-17 — Close the seam' and OPERATOR.md first. Each run do exactly ONE of these, checked in order, then stop.\n\n1) EPITAPHS: Find the newest reviews/foundry/round-NNN.json. If some reports/gauntlet-*.json covers all its idea names AND any name lacks an entry in reviews/foundry/dead-ideas.json, add one entry per missing idea: name; lineage 'foundry rNNN · <lens>'; tested (date + window); result (n, pnl, edge/slot, win rate); verdict 'dead' (or 'holdout_survivor' ONLY if it beat its own kill_criterion — never 'live_arm', going live is a human decision); an epitaph with the mechanism-level lesson; and a gradient object {predicted_fires_per_sym_day (from spec), actual, net_expectancy_pct}. Promote any pattern seen twice into failure_lessons. Maintain top-level total_ideas_tested. Validate JSON parses, commit, push.\n\n2) IMPLEMENT: Else if any idea from the newest round is missing from STRATEGY_VARIANTS in config.py: implement ALL missing ideas. Each is a pure function in signals.py, signature (bars, params, context) -> Signal | None, following the entry_rule verbatim (see existing foundry strategies in signals.py for idiom; use context.get('btc_bars') or context.get('system_state') only if the spec needs them, declaring matching context_keys). Register each in STRATEGY_REGISTRY and in config.STRATEGY_VARIANTS with enabled=False and the spec's params. Add unit tests following tests/test_signals.py TestFoundryRound001 patterns (insufficient-bars None, flat-series no-fire, one constructed fire case each). Run 'python -m pytest tests/ -q' and iterate until fully green. Commit with a message crediting the round, push.\n\n3) Else print the pipeline state and exit.\n\nHard rules: never set enabled=True on anything; never modify trader.db, .env, vps/, or the live roster; never force-push; only push with a green test suite."
}
